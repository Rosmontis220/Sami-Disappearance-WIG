<!-- 👁️我在看着你👁️ -->
# 知名主播萨米失联事件

《响石旅行社 · 萨米专区》网页解谜游戏（WIG）。基于《明日方舟》世界观，
整体由三个“站点”合并为三个 HTML，站内以 hash 视图切换，站内 BGM 不中断。

## 线上地址

- 入口（对外宣传引导页）：<https://rosmontis220.github.io/Sami-Disappearance-WIG/intro.html>
- 仓库根路径会自动跳转到引导页。

## 三个站点文件

| 文件 | 站点 | 内容 |
| --- | --- | --- |
| `travel.html` | 响石旅行社站 | 本体主线：官网公开页 → 直播存档 → 关键帧 → 档案 → 预言 → 日志 → 古树 → 结局与两个结局 |
| `minliang.html` | 皿良站 | DLC1《夺位》：咨询台 → 永远的第二 → Excel → 真相 → 利刃所指结局 |
| `miller.html` | 米勒调查站 | DLC2《越过群山》：E01 → E02 → E03 → 终章 |

`intro.html` 是对外引导页（不并入三站）；根目录 `index.html` 仅做跳转。

## 玩法

- 关键词不能点击，把页面里加粗/变色的词抄进搜索框才能推进；
- 搜索错误的词会进入对应站点的 404；
- 旅行社站与皿良站搜索词库互不相通；
- 皿良站为线性流程，跳过步骤搜索后置关键词会进 404；
- 部分内容需要全选页面才能看见（白色隐藏文字 / 古树同色刻痕）。

## 运行时说明

- `js/router.js`：三站合并壳层路由（hash 切换视图、跨站跳转、BGM 控制）；
- `js/main.js`：站内逻辑（搜索、谜题、结局状态）；
- 每站顶层按视图挂载多首 BGM，切换章节不中断；刷新后若浏览器拦截自动播放，
  点击页面任意位置即可唤醒；
- `assets/`、`css/` 为运行时资源。

## 本地运行

纯静态站点，直接打开 `intro.html`，或在项目目录启动任意静态服务器：

```bash
python -m http.server 8000
```

然后访问 <http://localhost:8000/intro.html>。

## 说明

- 本游戏为《明日方舟》世界观下的同人创作，与现实中的任何人、事、物无关；
- 旧版多页结构已从仓库移除，如需比对可参考 Git 历史或本地备份
  `../ZFQ_旧版多页备份/`；
- `大纲.md` 等文档含完整通关流程与剧透，属作者自用开发文档。
